# DIAGNOSTIC: scoped to the central, which is the half whose host link stalls.
# Adds connection events, parameter updates and disconnects to the USB log
# without the timing distortion full debug logging would cause.
CONFIG_BT_LOG_LEVEL_INF=y
